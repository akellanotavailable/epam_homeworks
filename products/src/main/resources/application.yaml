server:
  port: 8181

logging:
  level:
    org:
      springframework:
        web:
          servlet:
            DispatcherServlet: DEBUG

spring:
  application:
    name: products

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/