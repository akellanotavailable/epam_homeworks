server:
  port: 8282

logging:
  level:
    org:
      springframework:
        web:
          servlet:
            DispatcherServlet: DEBUG

spring:
  application:
    name: orders

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/