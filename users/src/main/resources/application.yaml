server:
  port: 8383

logging:
  level:
    org:
      springframework:
        web:
          servlet:
            DispatcherServlet: DEBUG

spring:
  application:
    name: users

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/